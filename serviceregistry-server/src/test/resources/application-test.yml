spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  main:
    lazy-initialization: 'true'
  datasource:
    url: jdbc:h2:mem:test
logging:
  level:
    root: DEBUG
    org:
      springframework: DEBUG
difi:
  move:
    sign:
      keystore:
        path: classpath:kontaktinfo-client-test.jks
        password: changeit
        alias: '991825827'
    fiks:
      oidc:
        keystore:
          path: changeit
          alias: changeit
          password: changeit
        client-id: changeit
        url: https://foo
        audience: https://foo
      svarut:
        serviceRecordUrl: https://foo
        certificate: classpath:test.pem
        base-url: https://foo
        integrasjon-id: <user>
        integrasjon-passord: <pass>
    healthcare:
      enabled: true
      nhn-adapter-endpoint-url: http://localhost:8089/arlookup/{identifier}
      fastlege-process: urn:no:difi:profile:digitalpost:fastlege:ver1.0
      nhn-process: urn:no:difi:profile:digitalpost:helse:ver1.0
server:
  port: '9099'
