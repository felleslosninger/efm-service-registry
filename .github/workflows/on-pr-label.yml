name: On PR label

on:
  pull_request:
    types: [labeled]

jobs:
  on-pr-label:
    permissions:
        pull-requests: write
    uses: felleslosninger/github-workflows/.github/workflows/on-pr-label.yml@main